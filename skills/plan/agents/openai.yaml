interface:
  display_name: "Plan"
  short_description: "Draft and finalize local execution plans"
  default_prompt: "Use $plan to explore a task, save a draft plan, and finalize it after confirmation."
